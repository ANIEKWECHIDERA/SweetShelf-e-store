[build]
  command = "rm -rf node_modules package-lock.json && npm install && npm run build --workspace storefront-web"
  publish = "apps/storefront-web/.next"

[build.environment]
  NODE_VERSION = "22"
